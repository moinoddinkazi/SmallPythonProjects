import numpy as no
import matplotlib.pyplot as plt
h=50
h1 = h/2
al-sqrt(3)/2
x=np.array([0,h1-al*h1, h1/2,h1,h1+(h1/2),a1*h1+h1,h])
theta1=linspace(0,120,7)
theta2=linspace(188,270,7)
fig = plt.subplots (figsize=(2, 8))
plt.grid(True)
plt.xlabel('Theta')
plt.ylabel('Lift')
plt.xticks(np.arange(0, 280, step=10))
plt.yticks (np.arange(0, 55, step-2))
pit.plot(thetal,x, theta2, flip(x))